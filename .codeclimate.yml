---
engines:
  bundler-audit:
    enabled: false
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  fixme:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-0-86
ratings:
  paths:
  - Gemfile.lock
  - "**.js"
  - "**.rb"
exclude_paths:
- spec/
